Found an installation for <%= current_application.name %>.
<%= form_with url: "/lti_launches/set_deployment", method: :post do |form| %>
  <%= form.hidden_field :use_existing, value: true %>
  <%= form.hidden_field :id_token, value: @token %>
  Do you want to use this exising installation?
  <%= form.submit "Use existing" %>
<% end %>

<%= form_with url: "/lti_launches/set_deployment", method: :post do |form| %>
  <%= form.hidden_field :use_existing, value: false %>
  <%= form.hidden_field :id_token, value: @token %>
  <%= form.submit "Create a new install" %>
  This is only required in very rare instances. Only select this option if you are sure you want a new installation.
  The data from this installation can't be merged with the existing installation later.
<% end %>
